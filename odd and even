// A voting system that checks if you are eligible to vote.
#include <stdio.h>

int main() {
    int num1;
    printf("Enter number: ");
    scanf("%d", &num1);
    
    (num1 % 2 ==0 ) ? printf("it is even") : printf("it is odd");
    return 0;
}
