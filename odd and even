// A voting system that checks if you are eligible to vote.
#include <stdio.h>

int main() {
    int num;
    printf("Enter number: ");
    scanf("%d", &num);
    
    (num % 2 ==0 ) ? printf("it is even") : printf("it is odd");
    return 0;
}
